<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

        <variable
            name="state"
            type="com.rud.movieapp.view.list.adapter.State" />

        <import type="com.rud.movieapp.view.list.adapter.State" />
    </data>

    <RelativeLayout
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="12dp">

    <ProgressBar
        android:id="@+id/progressBar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:viewVisibility="@{state == State.LOADING}"
        android:layout_centerInParent="true" />

    <TextView
        android:id="@+id/tvError"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        app:viewVisibility="@{state == State.ERROR}"
        android:text="@string/error_tap_to_retry" />

</RelativeLayout>
</layout>